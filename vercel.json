{
  "buildCommand": "prisma generate && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_WS_URL": "wss://infinite-server.fly.dev"
  }
}